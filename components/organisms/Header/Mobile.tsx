import React, { useState } from "react";

export default function MobileHeader() {
  const [name, setName] = useState<number>(0);
  useMemo(() => first, [second])
  return <div>this header for mobile</div>;
}
